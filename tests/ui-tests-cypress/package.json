{
  "name": "polls-app-ui-tests",
  "version": "1.0.0",
  "description": "Web ui functional tests for polls app",
  "main": "index.js",
  "dependencies": {
    "cypress": "^3.1.5",
    "mocha": "^5.2.0",
    "mochawesome": "^3.1.1",
    "mocha-junit-reporter": "^1.22.0",
    "mocha-multi-reporters": "^1.1.7",
    "mocha-allure-reporter": "^1.4.0"
  },
  "scripts": {
    "cypress:open": "./node_modules/.bin/cypress open",
    "delete:reports": "rm -rf cypress/results/* || true",
    "pretest": "npm run delete:reports",
    "test": "./node_modules/.bin/cypress run",
    "test chrome": "./node_modules/.bin/cypress run --browser chrome"
  },
  "author": "",
  "license": "ISC"
}
