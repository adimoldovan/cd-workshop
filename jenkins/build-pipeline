node("build-server") {
    try {
        stage("Build server") {
            build job: '../Build/build-server'
        }
        stage("Build client") {
            build job: '../Build/build-client'
        }
    }
    catch(Exception e) {
        println e
        currentBuild.result = "FAILURE"
    }
}
